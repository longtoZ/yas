apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: yas-observability-dashboard
spec:
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  json: |-
{{ .Files.Get "files/observability_dashboard.json" | indent 4 }}
