name: rating image ci

# On any branch push touching rating, build the image tagged with the commit id
# (and branch name) and push to Docker Hub. Reuses reusable-build-push-image.yaml.

on:
  push:
    paths:
      - 'rating/**'
      - 'pom.xml'
      - '.github/workflows/image-rating.yaml'
      - '.github/workflows/reusable-build-push-image.yaml'
  workflow_dispatch:

jobs:
  build-push:
    uses: ./.github/workflows/reusable-build-push-image.yaml
    with:
      service: rating
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
