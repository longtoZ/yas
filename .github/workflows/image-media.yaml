name: media image ci

# On any branch push touching media, build the image tagged with the commit id
# (and branch name) and push to Docker Hub. Reuses reusable-build-push-image.yaml.

on:
  push:
    paths:
      - 'media/**'
      - 'pom.xml'
      - '.github/workflows/image-media.yaml'
      - '.github/workflows/reusable-build-push-image.yaml'
  workflow_dispatch:

jobs:
  build-push:
    uses: ./.github/workflows/reusable-build-push-image.yaml
    with:
      service: media
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
